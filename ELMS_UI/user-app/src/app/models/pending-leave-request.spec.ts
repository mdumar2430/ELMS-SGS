import { PendingLeaveRequest } from './pending-leave-request';

describe('PendingLeaveRequest', () => {
  it('should create an instance', () => {
    expect(new PendingLeaveRequest()).toBeTruthy();
  });
});
